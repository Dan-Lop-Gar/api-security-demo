apiVersion: v1
kind: Service
metadata:
  name: resource-server-service
  namespace: default
spec:
  selector:
    app: resource-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  type: LoadBalancer