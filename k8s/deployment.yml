apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-server
  namespace: mi-empresa
spec:
  replicas: 3
  selector:
    matchLabels:
      app: resource-server
  template:
    metadata:
      labels:
        app: resource-server
    spec:
      containers:
        - name: resource-server
          image: danst21/resource-server:5776b7e14c35eeb46f786bf078a79fee38a78a36
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: resource-server-config
#            - secretRef:
#                name: resource-server-secrets   # variables sensibles
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"